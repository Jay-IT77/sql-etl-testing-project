# SQL + ETL Testing Project

## Overview
This project validates ETL data by comparing source and target datasets.

## Test Coverage
- Duplicate detection
- Null value validation
- Row count validation
- Transformation validation

## How to Run
pip install -r requirements.txt
python -m pytest -v
